# secrets 
.env

# python
__pycache__/
*.pyc
.venv/
venv/

# local data stores
data/
chroma_db/
*.sqlite
*.db

# os / editor
.DS_Store
.idea/
.vscode/

# personal notes — never part of the product
learn.md
agent-handoff.md

# local debug artifacts
.playwright-mcp/
/react-5173.png
/streamlit-8501.png
/streamlit-check.png

# diagram sketches - regenerate rather than commit; the docs/ SVGs are canonical
/arch.png
/tech.png
/tech-plain.png
/crosswalk.png
/diagrams.md

# cloudflare deploy attempt - torn down, kept out of the repo
/worker/
.wrangler/
.dev.vars

# iCloud conflict copies ("file 2.py") - never real files, never committed
* 2
* 2.*

# crosswalk draft - review it, promote it, do not commit it
backend/rag/crosswalk.draft.json

# coverage artifacts
.coverage
htmlcov/
.pytest_cache/
