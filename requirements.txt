streamlit>=1.37
requests>=2.32
tzdata # zoneinfo's database - present on macOS, absent in slim containers
fastapi
uvicorn
anthropic
apscheduler

# RAG dependencies:
llama-index
llama-index-core
llama-index-vector-stores-chroma
llama-index-embeddings-huggingface
llama-index-readers-file  # PDF loading for the doc lane (brings pypdf)
chromadb
sentence-transformers<5
transformers<5
# Intel Macs: torch stopped publishing wheels for this platform after 2.2.2,
# and sentence-transformers>=5 / transformers>=5 require torch>=2.5, which
# has no wheel here - pinned below torch 2.2.2's ceiling. numpy<2 matches
# torch 2.2.2's build ABI (numpy 2.x raises "_ARRAY_API not found").
# Skip both pins on Apple Silicon or Linux, where newer torch wheels exist.
numpy<2